# ラケット診断ツール 仕様書 (racket-finder)

## 0. この文書について

- この文書は Claude Code に読み込ませる開発指示書である
- 実装は「7. 実装順序」に従って段階的に進めること
- 数値定数は設計上の初期値であり、後で調整する前提。**マジックナンバーをロジック内に直書きせず、必ず定数ファイルに切り出すこと**

---

## 1. 目的とスコープ

### 目的

8問の質問に答えると、ユーザーに合ったテニスラケットを3本提案する単一ページのWebツール。

### ビジネス上のゴール

- 提案した各ラケットにアフィリエイトリンクを設置し、収益化する
- 検索流入（「テニスラケット 選び方」「ラケット 診断」等）を狙うため、SEOを意識した静的生成にする

### Phase 1 のスコープ（今回作るもの）

- 質問フォーム（8問）
- 診断ロジック
- 結果表示（3本＋レーダーチャート＋理由文）
- ラケットデータ 30〜50本（JSON）
- 結果のシェアURL

### スコープ外（作らないもの）

- ユーザー登録・ログイン
- データベース
- レビュー投稿機能
- 管理画面

DBもAPIも不要。すべて静的データとクライアントサイド計算で完結させる。

---

## 2. 技術スタック

- Next.js 14+ (App Router)
- TypeScript
- Tailwind CSS
- shadcn/ui
- Recharts（レーダーチャート）
- ラケットデータは `src/data/rackets.json`
- ホスティングは Vercel を想定

---

## 3. データモデル

### 3.1 ラケットの生スペック

```ts
type RacketSpec = {
  id: string;              // 例: "yonex-ezone-100-2026"
  brand: string;
  series: string;          // 例: "EZONE"  ※多様化フィルタで使用
  model: string;           // 例: "EZONE 100"
  year: number;

  weight: number;          // フレーム重量(g)
  balance: number;         // バランス(mm)
  swingWeight: number;     // スイングウェイト
  headSize: number;        // フェイス面積(inch^2)
  ra: number;              // フレーム硬度(RA)
  beamWidth: number;       // フレーム厚 最厚部(mm)
  pattern: '16x19' | '16x20' | '18x20' | 'other';

  price: number;           // 実売価格の目安(円)
  imageUrl: string;
  affiliateUrl: {
    rakuten: string;
    amazon: string;
  };
};
```

### 3.2 6軸スコア（算出値）

```ts
type AxisScores = {
  power: number;        // 0-100
  control: number;
  spin: number;
  maneuverability: number;  // 操作性
  comfort: number;          // 快適性
  volley: number;
};

const AXES = ['power','control','spin','maneuverability','comfort','volley'] as const;
type Axis = typeof AXES[number];
```

### 3.3 重要: 6軸スコアの算出方法

**6軸スコアを手作業でJSONに書かないこと。** 生スペックから計算で導出する。

理由: 50本を手で採点すると評価がブレて、診断結果の一貫性が失われるため。

#### 正規化のルール（必須）

各スペック値は **絶対値ではなく、手持ちデータ内での相対順位** で 0〜100 に正規化する。

```ts
// データセット内の min/max で線形正規化する
function norm(value: number, all: number[]): number {
  const min = Math.min(...all), max = Math.max(...all);
  return max === min ? 50 : ((value - min) / (max - min)) * 100;
}
```

「RA 65 なら快適性70点」のような絶対マッピングは禁止。データを追加するたびに全体が破綻するため。

#### 各軸の算出式（初期値）

`n()` は上記の正規化、`inv()` は `100 - n()` とする。

```
power           = 0.35*n(swingWeight) + 0.25*n(headSize) + 0.25*n(ra) + 0.15*n(beamWidth)
control         = 0.35*inv(headSize)  + 0.30*inv(ra)     + 0.20*patternControlBonus + 0.15*inv(beamWidth)
spin            = 0.40*patternSpinBonus + 0.30*n(headSize) + 0.30*n(swingWeight)
maneuverability = 0.45*inv(weight)   + 0.40*inv(swingWeight) + 0.15*inv(balance)
comfort         = 0.45*inv(ra)       + 0.30*inv(beamWidth)   + 0.25*n(weight)
volley          = 0.35*inv(balance)  + 0.35*maneuverability  + 0.30*n(weight)
```

パターンボーナス:

| pattern | patternControlBonus | patternSpinBonus |
|---|---|---|
| 18x20 | 100 | 20 |
| 16x20 | 65 | 55 |
| 16x19 | 35 | 100 |
| other | 50 | 50 |

comfort の `n(weight)` が正である点に注意（重い方が衝撃を吸収するため）。

#### 手動オーバーライド

式で出した値が実感と明らかに違う機種のみ、JSONに `overrides` を持たせて上書きできるようにする。

```ts
overrides?: Partial<AxisScores>;
```

多用しないこと。10本を超えたら式の方を見直す。

---

## 4. 質問と重み対応表

### 4.1 全体の考え方

1. **Q1（レベル）で理想値と重みの基準を決める**
2. **Q2〜Q7 が基準を上書きする**（理想値は加算、重みは乗算）
3. Q4〜Q6, Q8 はハードフィルタも持つ

```ts
type Target = {
  ideal: AxisScores;              // 理想の6軸値
  weight: Record<Axis, number>;   // 各軸の重視度
  filters: HardFilter[];
};
```

### 4.2 Q1 レベル（基準値）

質問文: 「テニス歴・レベルを教えてください」

**理想値 BASE_IDEAL**

| レベル | power | control | spin | maneuver | comfort | volley |
|---|---|---|---|---|---|---|
| 初級（〜1年 / 週1未満） | 75 | 40 | 45 | 70 | 75 | 50 |
| 初中級（1〜3年） | 68 | 50 | 55 | 62 | 68 | 52 |
| 中級（3〜10年 / 草トー出る） | 55 | 62 | 65 | 55 | 58 | 55 |
| 中上級以上（10年〜 / 試合中心） | 45 | 75 | 70 | 48 | 45 | 58 |

**重み BASE_WEIGHT**

| レベル | power | control | spin | maneuver | comfort | volley |
|---|---|---|---|---|---|---|
| 初級 | 1.2 | 0.6 | 0.6 | 1.5 | 1.5 | 0.8 |
| 初中級 | 1.1 | 0.9 | 1.0 | 1.2 | 1.2 | 0.9 |
| 中級 | 1.0 | 1.2 | 1.2 | 1.0 | 1.0 | 1.0 |
| 中上級以上 | 0.9 | 1.5 | 1.3 | 0.9 | 0.8 | 1.1 |

### 4.3 Q2 プレースタイル

質問文: 「どんなプレーが多いですか」

| 回答 | 理想値の差分 | 重み倍率 |
|---|---|---|
| ベースライン中心 | spin +8 | spin ×1.5 / power ×1.2 / volley ×0.5 |
| オールラウンド | — | — |
| ネット中心・ダブルス主体 | volley +15 / maneuver +8 | volley ×2.0 / maneuver ×1.4 / spin ×0.6 |

### 4.4 Q3 スイングの大きさ

質問文: 「スイングはどちらに近いですか」

レベルとは独立に効かせること。上級者ほどパワーを求めないため、レベルだけでパワー理想値を決めると逆方向に外れる。

| 回答 | 理想値の差分 | 重み倍率 |
|---|---|---|
| コンパクト（当てる・ブロック気味） | power +15 / comfort +5 | power ×1.3 |
| 標準 | — | — |
| フルスイング（大きく振り切る） | power −18 / control +10 / spin +8 | control ×1.2 |

### 4.5 Q4 いま一番困っていること【最重要】

質問文: 「いま一番困っていることは？（最大2つまで）」

- 1つ目の選択肢は下表の倍率をそのまま適用
- 2つ目の選択肢は倍率を **×1.5 に固定** して適用
- 3つ以上は選択させないこと（全軸の重みが上がり、重み無しと同じ結果になるため）

| 回答 | 理想値の差分 | 重み倍率 | ハードフィルタ |
|---|---|---|---|
| ボールが飛ばない | power +15 | power ×2.5 | — |
| 飛びすぎる・アウトする | power −20 / control +15 | control ×2.5 | — |
| 回転がかからない | spin +15 | spin ×2.5 | pattern が 18x20 以外 |
| 振り遅れる | maneuver +15 | maneuver ×2.5 | weight ≤ Q6の現在重量 |
| 手や腕に衝撃が響く | comfort +15 | comfort ×2.5 | — |

### 4.6 Q5 肘・肩の不安

質問文: 「肘や肩に不安はありますか」

| 回答 | 理想値の差分 | 重み倍率 | ハードフィルタ |
|---|---|---|---|
| 特にない | — | — | — |
| たまに気になる | comfort +8 | comfort ×1.8 | ra ≤ 70 |
| 痛みがある / 治療中 | comfort +15 | comfort ×3.0 | ra ≤ 66 かつ beamWidth ≤ 24 かつ **weight ≥ 285** |

**「痛みがある」の重量下限 285g は意図的な仕様。削除しないこと。**
軽いラケットほど打球衝撃が腕に伝わりやすく、肘の症状を悪化させるため。ユーザーの直感に反するので、結果画面に必ず以下の趣旨の注記を表示する:

> 肘に不安がある場合、軽いラケットの方が負担が少なそうに感じますが、実際は軽いほど衝撃が腕に伝わります。ある程度の重量があり、フレームが柔らかいモデルを選んでいます。

なお本ツールは医療的助言を行うものではない。痛みが続く場合は医療機関の受診を勧める一文を併記すること。

### 4.7 Q6 現在使っているラケットの重量

質問文: 「いま使っているラケットの重さは？」

理想値・重みには影響させず、ハードフィルタ専用。

| 回答 | フィルタ |
|---|---|
| 〜275g | weight 255〜290 |
| 275〜290g | weight 265〜300 |
| 290〜305g | weight 280〜315 |
| 305g〜 | weight 295〜330 |
| わからない / 持っていない | Q1×Q3 から推定（下表） |

上限側を狭くしているのは、重すぎる失敗の方が実害が大きいため。

推定表（「わからない」の場合）:

| | コンパクト | 標準 | フルスイング |
|---|---|---|---|
| 初級 | 265〜290 | 270〜295 | 275〜300 |
| 初中級 | 270〜295 | 280〜300 | 285〜305 |
| 中級 | 280〜300 | 285〜305 | 290〜315 |
| 中上級以上 | 285〜305 | 295〜315 | 300〜330 |

### 4.8 Q7 張るストリング

質問文: 「張る予定のストリングは？」

ポリは飛ばず硬いため、ラケット側で補わせる。

| 回答 | 理想値の差分 |
|---|---|
| ポリエステル | power +10 / comfort +8 |
| ナイロン・マルチフィラメント | power −5 / control +5 |
| わからない | ナイロンと同じ扱い |

### 4.9 Q8 予算

質問文: 「予算の上限は？」

| 回答 | フィルタ |
|---|---|
| 〜20,000円 / 〜30,000円 / 〜40,000円 / 上限なし | price ≤ 上限 |

**予算内に候補が無い場合、条件を勝手に緩めないこと。**「ご予算内では条件に合うモデルが見つかりませんでした。◯◯円まで広げると3本見つかります」と正直に表示し、予算を引き上げるボタンを出す。

---

## 5. スコアリング

### 5.1 処理の流れ

```ts
// 1. 基準
let ideal  = { ...BASE_IDEAL[answers.q1] };
let weight = { ...BASE_WEIGHT[answers.q1] };

// 2. Q2〜Q5, Q7 を順に適用（Q4は選択肢ごと）
applyDelta(ideal, ...);   // 加算
applyMul(weight, ...);    // 乗算

// 3. 理想値を 0-100 にクランプ
clamp(ideal, 0, 100);

// 4. 重みを正規化（合計を 6.0 に揃える）※必須
normalizeWeight(weight, 6.0);

// 5. ハードフィルタで候補を絞る
const candidates = rackets.filter(r => passAllFilters(r, filters));

// 6. スコアリング
const scored = candidates.map(r => ({ racket: r, score: score(r, target) }));

// 7. 多様化して3本選ぶ
const result = diversify(scored);
```

重みの正規化は必須。倍率を掛けっぱなしだと、Q4とQ5が両方強く効いた人だけ適合度の絶対値が下がり、「適合度 %」表示が不自然になる。

### 5.2 スコア計算（減点方式）

```ts
const ONE_SIDED: Axis[] = ['comfort', 'maneuverability', 'volley'];

function score(racket: AxisScores, target: Target): number {
  let penalty = 0;
  for (const axis of AXES) {
    const diff = target.ideal[axis] - racket[axis];
    // 快適性・操作性・ボレーは「足りない」ときだけ減点する。
    // 超過は問題にならないため、両側減点にすると不当に落ちる。
    const gap = ONE_SIDED.includes(axis) ? Math.max(0, diff) : Math.abs(diff);
    penalty += target.weight[axis] * gap;
  }
  return Math.max(0, 100 - penalty / 6.0);
}
```

**この非対称ペナルティは結果の納得感を大きく左右する。両側絶対値に変更しないこと。**

power と control はほぼ逆相関なので、両方を高い理想値にすると全ラケットが低得点になる。Q3・Q4 で必ずどちらかに寄せる設計になっている。

### 5.3 多様化（3本の選び方）

素直に上位3件を返すと同シリーズの重量違いが並び、診断された感が失われる。

ルール:

1. **同一 `series` からは1本まで**
2. 1位 = 最高スコアのもの
3. 2位 = 残りのうち「やさしめ寄り」（comfort + maneuverability が高い方）で最高スコアのもの
4. 3位 = 残りのうち「攻撃的寄り」（control + spin が高い方）で最高スコアのもの
5. 候補が3本に満たない場合は、あるだけ表示する（水増ししない）

ラベル: 1位「総合ベストマッチ」/ 2位「もっと扱いやすく」/ 3位「もっと攻めたいなら」

---

## 6. 結果画面

### 6.1 表示内容

- 3本のカード（画像 / モデル名 / 適合度% / スペック要約 / 理由文 / 購入リンク）
- レーダーチャート: **ユーザーの理想値と各ラケットの6軸を重ねて描画**（Recharts）
- Q5で「痛みがある」を選んだ場合の注記（4.6参照）
- 3本を並べた簡易比較表

### 6.2 理由文

LLMは使わない。テンプレートで生成する。

手順:
1. 正規化後の `weight` が大きい軸を上位2つ取る
2. その軸について、ラケットの該当スコアが高いことを述べる定型文を組み合わせる

```
「{困りごとの言い換え}なので、{軸1の特徴}な{モデル名}を選びました。{軸2の特徴}なので、{副次的な効果}も期待できます。」
```

例:
> 肘への負担を気にされているので、フレームが柔らかく衝撃の少ない◯◯を選びました。振り抜きも軽いので、振り遅れの解消にもつながります。

### 6.3 アフィリエイト表記【法令対応・必須】

景品表示法のステルスマーケティング規制（2023年10月施行）への対応として、アフィリエイトリンクを含むページには広告である旨を明示する。

- **ページ最上部**に「本ページはアフィリエイト広告を含みます」を常時表示する
- 折りたたみ・小さすぎる文字・スクロールしないと見えない位置は不可
- 購入ボタン近傍にも「PR」を併記する

### 6.4 シェアURL

回答をクエリパラメータにエンコードし、同じ結果を再現できるURLを発行する（例: `/result?a=2-1-3-4,2-2-3-2`）。
X等での拡散が主要な流入源になるため、OGP画像とタイトルも動的に設定すること。

---

## 6.5 ディレクトリ構成と規約

判断に迷って質問することを避けるため、以下に従うこと。ここに書かれていない些細な選択は、既存の慣習に合わせて自分で決めてよい。

```
src/
  app/
    page.tsx              # トップ（質問フォーム）
    result/page.tsx       # 結果画面
    layout.tsx
  components/
    QuestionStep.tsx
    ResultCard.tsx
    AxisRadarChart.tsx
    ComparisonTable.tsx
    ui/                   # shadcn/ui の生成物
  lib/
    types.ts              # 型定義
    constants.ts          # 第4章の対応表（BASE_IDEAL 等）
    axes.ts               # 生スペック → 6軸スコア
    target.ts             # 回答 → ideal / weight / filters
    score.ts              # スコアリングと多様化
    reason.ts             # 理由文テンプレート
    share.ts              # 回答のURLエンコード/デコード
    __tests__/
  data/
    rackets.json
```

規約:

- コンポーネントは PascalCase、それ以外のファイルは camelCase
- 型は `type` を使う（`interface` は使わない）
- ロジック層（`src/lib/`）は React に依存させない。純粋関数のみで書き、単体テスト可能にすること
- テストランナーは Vitest
- 状態管理ライブラリは導入しない。`useState` と URL クエリで足りる
- 日本語UI。文言は各コンポーネントに直書きでよい（i18n は不要）

## 7. 実装順序

原則として、各ステップの完了を報告したうえで確認を待たずに次へ進んでよい。
ただし **ステップ3の完了時のみ、必ず手を止めて人間の確認を待つこと**（理由は該当箇所に記載）。

1. **プロジェクト初期化** — Next.js + TypeScript + Tailwind + shadcn/ui
2. **型定義と定数ファイル** — `src/lib/types.ts`, `src/lib/constants.ts`（第4章の表をそのまま定数化）
3. **6軸算出ロジック** — `src/lib/axes.ts`。ラケット10本の仮データで動かし、全ラケット×6軸のスコアを表形式で出力する
   **【確認ポイント】ここで必ず停止すること。** 算出結果を人間が見て妥当性を判断する。ここが狂ったまま進むと後続が全てやり直しになるため、承認を得るまで次に進まない
4. **ターゲット算出ロジック** — `src/lib/target.ts`（回答 → ideal / weight / filters）
5. **スコアリングと多様化** — `src/lib/score.ts`
6. **テストケース実装**（第8章）— ここまでUIなしで完結させる
7. **質問フォームUI** — 1問1画面、プログレスバー付き
8. **結果画面UI** — カード、レーダーチャート、理由文
9. **ラケットデータを30〜50本に拡充**
10. **シェアURL / OGP / アフィリエイト表記**

**ステップ6までUIを作らないこと。** ロジックの妥当性を先に確定させる。

---

## 8. テストケース

`src/lib/__tests__/` に実装する。重み調整時のデグレ検知が目的。

| # | 入力 | 期待される挙動 |
|---|---|---|
| 1 | 初中級 / ベースライン / 標準 / 飛ばない / 不安なし / 285-300g / ナイロン / 〜30,000円 | フェイス大きめ・スイングウェイト高めの入門〜中級モデルが1位 |
| 2 | 中上級 / ベースライン / フルスイング / 飛びすぎる / 不安なし / 305g〜 / ポリ / 上限なし | 薄ラケ・小さめフェイス・18x20 寄りが1位 |
| 3 | 中級 / フルスイング / 飛ばない | 理想 power が 55−18+15 = 52。極端なパワーラケットが**選ばれない**こと。スピン側で解決に向かう |
| 4 | 初級 / 痛みがある | 全候補が ra ≤ 66 かつ weight ≥ 285。候補が0本ならフィルタが厳しすぎるサイン |
| 5 | 痛みがある + 振り遅れる | comfort（重め）と maneuver（軽め）が拮抗。結果が2本以下になる場合はデータ不足を疑う |
| 6 | ネット中心 / 中級 | volley 上位のトップライトモデルが1位 |
| 7 | 予算 〜20,000円 / 中上級 | 候補0のときにエラーではなく「予算を上げる提案」が返る |
| 8 | 全問で最も極端な回答の組み合わせ | クラッシュせず、必ず1本以上返るか、明示的な「該当なし」を返す |
| 9 | 任意の同一入力を2回 | 完全に同じ結果（乱数を使っていないこと） |
| 10 | 上位3本 | series が重複していない |

---

## 9. 実装上の注意（まとめ）

- 6軸スコアはJSONに手書きせず式で算出する（3.3）
- 正規化はデータセット内の相対値で行う（3.3）
- 重みは最後に正規化する（5.1）
- 快適性・操作性・ボレーは片側ペナルティにする（5.2）
- 「痛みがある」の重量下限 285g は意図的な仕様（4.6）
- 予算オーバー時に条件を勝手に緩めない（4.9）
- 同一シリーズは3本の中に1本まで（5.3）
- ステマ規制対応の広告表記を必ず入れる（6.3）
